# @sibility/core

Shared types, manifest schema, and playback utilities for Sibility.

Browser-safe — no Node.js filesystem APIs.

## Usage

```ts
import { defineConfig, parseManifest, findActiveWordIndex } from '@sibility/core';
```
